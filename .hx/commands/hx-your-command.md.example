---
name: hx-your-command
description: 示例项目级命令
usage: hx-your-command [options]
hooks: [pre, post]
---

# 示例命令

## 目标

- 描述这个命令唯一负责的动作。

## 何时使用

- 适用场景:
- 不适用场景:

## 输入

- 命令参数: `$ARGUMENTS`
- 必选参数: 无
- 可选参数: 无
- 默认值: 无
- 依赖输入: 当前项目真实文件、目录和配置

## 执行步骤

1. 读取必要输入并校验前置条件。
2. 只在拥有写权的对象上执行变更。
3. 输出结构化结果。

## 成功结果

- 返回本命令的可复用产物。

## 失败边界

- 缺少关键输入，或当前对象不允许安全修改。

## 下一步

- 指向后续命令或人工确认动作。

## 约束

- 不虚构项目事实。
- 不覆盖用户已有人工内容。
