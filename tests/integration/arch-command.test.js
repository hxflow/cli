// hx-arch-test.js 已在当前版本中移除。
// 架构合规检查通过 /hx-gate Claude 命令执行，不再是独立脚本。
import { describe, it } from 'vitest'

describe('hx:arch command（已移除）', () => {
  it.todo('hx-arch-test.js 已移除，架构检查通过 /hx-gate 命令执行')
})
